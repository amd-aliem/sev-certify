# Target reached when all the other stages have completed.
# Job services should use: WantedBy=stop.target

[Unit]
Description=Stop stage

After=test.target
Requires=test.target

Wants=stop-done.service
After=stop-done.service


[Install]
WantedBy=multi-user.target

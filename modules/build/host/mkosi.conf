[Include]
Include=./beacon
Include=./embed-guest-image
Include=./snphost
Include=./format-qemu-binary
Include=./targets
Include=./pip-install
Include=./guest-logs-directory
Include=../common
Include=../../system/host
Include=../../test/host
Include=../../stop/host
